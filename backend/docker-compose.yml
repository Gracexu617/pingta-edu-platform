services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: education_intel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d education_intel"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
